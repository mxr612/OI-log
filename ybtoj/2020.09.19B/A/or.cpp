/**
 * ybtoj2020.09.19B
 * A或位运算or
*/

#define MXN (100020)

#include <stdio.h>

int n, m;
int l[MXN], r[MXN], p[MXN];

signed
main() {
    freopen("or.in", "r", stdin);
#ifdef ONLINE_JUDGE
    freopen("or.out", "w", stdout);
#endif

    scanf("%d%d", &n, &m);

    for (int i = 0; i < m; ++i) {
    }

    return 0;
}