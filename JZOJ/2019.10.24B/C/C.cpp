// C

#include <stdio.h>
#include <string.h>

#include <iostream>
using namespace std;

typedef long long LL;

LL N, M, EN[4000000], dp[4000000];

int main() {

	freopen("C.in", "r", stdin);

	cin >> N >> M;
	for (int i = 1; i <= N; ++i)
		cin >> EN[i];

	memset(dp, 0x6f, sizeof(dp));
	dp[0] = M;

	for(int i=1;i<=N;++i){
		for(int j=){
			
		}
	}

	return 0;

}