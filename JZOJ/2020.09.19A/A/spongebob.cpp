/**
 * JZOJ2020.09.19A
 * A spongebob
 * 每条直线在零点处反转, 对于直线的零点集, 相邻两个零点间的函数是单调的
 * 但是SPJ被"nan"水了
*/

#include <stdio.h>
#include <stdlib.h>

#include <algorithm>

signed main() {
    freopen("spongebob.in", "r", stdin);
#ifdef ONLINE_JUDGE
    freopen("spongebob.out", "w", stdout);
#endif

    int n;

    printf("nan");

    return 0;
}