/**
 * P3378
 * heap
 */

#include <stdio.h>
#include <stdlib.h>

signed main() {

	freopen("P3378.in", "r", stdin);

	int N, o, x;

	scanf("%d", &N);

	for (register int i = 0; i < N; ++i) {
		scanf("%d", &o);
		switch (o) {
		case 1:
			scanf("%d", &x);
			break;
		case 2:
			break;
		case 3:
			break;
		default:;
		}
	}

	return 0;

}